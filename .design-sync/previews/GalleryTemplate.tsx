import { GalleryTemplate } from 'react-os-shell';

// GalleryTemplate — media grid with a Tabs filter and Card tiles carrying a
// StatusBadge overlay.

export function Gallery() {
  return (
    <div style={{ height: 620 }}>
      <GalleryTemplate />
    </div>
  );
}
