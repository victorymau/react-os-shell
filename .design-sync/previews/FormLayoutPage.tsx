import { FormLayoutPage } from 'react-os-shell';

// FormLayoutPage — settings form: grouped Cards of FormFields wrapping the
// kit's inputs, with an action row. The canonical "real form" reference.

export function Settings() {
  return (
    <div style={{ height: 760 }}>
      <FormLayoutPage />
    </div>
  );
}
